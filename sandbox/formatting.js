formattedString = `A hill
and a landfill,
	after it snows,
both softly glow
	under the moon.

But it won't last forever - 
	"Never, never, never!"`

jsonData = {
    "text": formattedString,
    "moreText": `Duuuude!!!    
            asfasdf`
}

console.log(jsonData.text)
console.log("@@@@@@@@@@@@@@@@@")
console.log(jsonData.moreText)